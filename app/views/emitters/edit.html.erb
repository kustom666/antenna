<fieldset>
	<div class="widget">
	    <div class="head">
	      <h5 class="iCoverflow">Emitter's properties</h5>
	    </div>
		<%= render 'form' %>

		<%= link_to 'Show', @emitter %> |
		<%= link_to 'Back', emitters_path %>
	</div>
</fieldset>